<% if @product.erros.empty?%>

    $('table tbody').append('<%= j render "admin/row", product: @product %>');
    $('.alert-success').html("El producto ha sido creado con éxito").show();
    $('#product-modal').modal('hide');
<% else %>
   
    $('<%= @product.errors.full_messages[0] %>');
<% end %>